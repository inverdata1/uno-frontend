import { Stack } from 'expo-router';

/**
 * Business Section Root Layout
 * Wraps the tabs layout
 */
export default function BusinessLayout() {
  return <Stack screenOptions={{ headerShown: false }} />;
}
