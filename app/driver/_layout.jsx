import { Stack } from 'expo-router';

/**
 * Driver Section Root Layout
 * Wraps the tabs layout
 */
export default function DriverLayout() {
  return <Stack screenOptions={{ headerShown: false }} />;
}
