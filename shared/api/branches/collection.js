/**
 * Branches collection name
 *
 * Branches represent physical locations of a business.
 * Every business has at least one branch (the default "Principal" branch).
 */
export const COLLECTION_NAME = 'branches';
